<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Modern CSS</title>
    <style>
      body {
        height: 100vh;
        width: 100vw;
        display: flex;
        justify-content: center;
        flex-flow: column;
        align-items: center;
        font-family: "Avenir Next", "Helvetica Neue", sans-serif;
        color: #444;
        margin: 0;
      }

      h1 {
        font-weight: 300;
      }

      a {
        font-weight: 100;
        font-size: 2em;
        font-weight: 500;
        text-decoration: none;
        color: inherit;
        display: block;
        padding: 1rem;
        border: 0.0625rem solid #ccc;
        background-color: #fafafa;
        border-radius: 0.5em;
        transition: all ease 250ms;
      }

      a + a {
        margin-top: 1em;
      }

      a:hover {
        color: #222;
        border-color: dodgerblue;
        background-color: white;
        box-shadow: 0 0 0 0.125em dodgerblue;
      }

      small {
        text-transform: lowercase;
        font-variant: small-caps;
      }
    </style>
  </head>
  <body>
    {{{ body }}}
  </body>
</html>
